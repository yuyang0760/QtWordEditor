# 功能实现记录

## 2026-02-23 01:00:00
### 实现内容
完全删除旧版 Span 类，完成从旧 Span 系统到新 InlineSpan 系统的完整迁移：
1. 修改 MainWindow.cpp 使用新的 InlineSpan 系统
2. 从 ParagraphBlock 中移除所有旧的 Span 兼容接口
3. 删除旧的 Span 类（Span.h 和 Span.cpp）
4. 修改 FormatController 使用新的 InlineSpan 系统
5. 确保项目可以正常编译通过

### 主要修改
1. **修改 MainWindow.cpp**：
   - 移除对 Span.h 的包含，添加对 InlineSpan.h 和 TextSpan.h 的包含
   - 使用 inlineSpanCount() 替代 spanCount()
   - 使用 findInlineSpanIndex() 替代 findSpanIndex()
   - 使用 inlineSpan() 替代 span()
   - 更新 spanInfo 显示内容，添加类型信息（文本/公式）
2. **修改 ParagraphBlock**：
   - 从 ParagraphBlock.h 中移除对 Span.h 的包含
   - 移除旧的接口方法：spanCount()、span()、addSpan()、setSpan()、findSpanIndex()
   - 移除私有成员变量 m_spans
   - 移除私有方法 mergeAdjacentSpans()
   - 更新拷贝构造函数和 clone() 方法，移除对 m_spans 的处理
3. **删除旧版 Span 类**：
   - 删除 Span.h 文件
   - 删除 Span.cpp 文件
4. **修改 FormatController.cpp**：
   - getCurrentDisplayStyle() 方法：使用 inlineSpanCount() 和 inlineSpan()，并判断 span 类型为 Text 时才获取样式
   - collectSelectionStyles() 方法：使用 inlineSpanCount() 和 inlineSpan()，并判断 span 类型为 Text 时才添加样式到结果

### 修改文件
- `src/ui/mainwindow/MainWindow.cpp`
- `include/core/document/ParagraphBlock.h`
- `src/core/document/ParagraphBlock.cpp`
- `src/editcontrol/formatting/FormatController.cpp`

### 删除文件
- `include/core/document/Span.h`
- `src/core/document/Span.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-23 00:45:00
### 实现内容
继续迁移到新的 InlineSpan 体系，完成了命令类的迁移：
1. 修改 SetCharacterStyleCommand：完全使用新的 InlineSpan 系统替代旧的 Span 系统
2. 修改 RemoveTextCommand：完全使用新的 InlineSpan 系统替代旧的 Span 系统
3. 确保项目可以正常编译通过

### 主要修改
1. **修改 SetCharacterStyleCommand**：
   - 将 m_oldSpans 从 QList<Span> 改为 QList<InlineSpan*>
   - 在析构函数中添加 qDeleteAll 清理内存
   - redo() 方法使用 clone() 克隆 InlineSpan 保存用于撤销
   - undo() 方法使用 clearInlineSpans() 清理并恢复旧的 spans
2. **修改 RemoveTextCommand**：
   - 移除 m_removedText 和 m_removedSpans
   - 添加 m_oldBlock（ParagraphBlock*）用于保存整个旧块的克隆
   - redo() 方法使用 clone() 克隆整个 ParagraphBlock 保存用于撤销
   - undo() 方法使用 clearInlineSpans() 清理并恢复旧的块内容和样式

### 修改文件
- `include/core/commands/SetCharacterStyleCommand.h`
- `src/core/commands/SetCharacterStyleCommand.cpp`
- `include/core/commands/RemoveTextCommand.h`
- `src/core/commands/RemoveTextCommand.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 22:00:00
### 实现内容
继续迁移到新的 InlineSpan 体系，完成了以下任务：
1. 修改 ParagraphBlock：暂时保留旧的兼容接口（使用旧版 m_spans），同时保留新的 InlineSpan 体系实现
2. 修改 TextBlockItem：完全重写使用新的 TextBlockLayoutEngine 进行布局
3. 确保项目可以正常编译通过

### 主要修改
1. **修改 ParagraphBlock**：
   - 暂时保留旧的兼容接口（spanCount、span、addSpan、setSpan、findSpanIndex）
   - 保留完整的新 InlineSpan 体系实现（inlineSpanCount、inlineSpan、addInlineSpan 等）
   - text()、setText()、insert()、remove() 等方法暂时还是使用旧的 m_spans
2. **完全重写 TextBlockItem**：
   - 移除了 m_textItem（QGraphicsTextItem）相关的代码
   - 集成了 TextBlockLayoutEngine
   - 实现了 createContentItemsFromBlock、performLayout 等方法
   - 当前 createContentItemsFromBlock 暂时还是使用旧的 m_spans

### 修改文件
- `include/core/document/ParagraphBlock.h`
- `src/core/document/ParagraphBlock.cpp`
- `include/graphics/items/TextBlockItem.h`
- `src/graphics/items/TextBlockItem.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 21:30:00
### 实现内容
完善 TextSpan 类，迁移旧版 Span 的所有功能，为删除旧版 Span 做准备。

### 主要修改
1. **在 TextSpan.h 中添加所有旧版 Span 的功能**：
   - 样式管理：styleName、setStyleName、directStyle、setDirectStyle、effectiveStyle
   - 分割操作：split(int position)
   - 比较操作：operator==、operator!=
   - 内部方法：updateFont() 用于更新字体
2. **修改 TextSpan.cpp 实现所有新功能**：
   - 从旧版 Span.cpp 完整迁移所有功能
   - 保持向后兼容性，style() 方法返回 directStyle
   - 实现 effectiveStyle() 支持 StyleManager
   - split() 返回 TextSpan*（新的 InlineSpan 体系）

### 修改文件
- `include/core/document/TextSpan.h`
- `src/core/document/TextSpan.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 21:00:00
### 实现内容
完成数学公式编辑器阶段六：统一光标实现。按照统一光标设计文档实现了完整的统一光标系统。

### 主要修改
1. **创建 UnifiedCursor 类**：统一光标类，继承 QObject
   - 实现光标状态管理（CursorState 结构体）
   - 实现光标移动（moveLeft、moveRight、moveUp、moveDown）
   - 实现光标渲染（updateCursor）
   - 实现编辑操作（insertText、deleteChar、backspace）
   - 提供信号（positionChanged、contentChanged）
2. **为 TextFragment 类添加光标定位方法**：
   - cursorPosition(int charOffset)：获取指定字符偏移处的光标位置
   - cursorHeight()：获取光标高度
3. **为 MathFormulaItem 类添加光标定位方法**：
   - cursorPosition(int childIndex)：获取指定子项索引处的光标位置
   - cursorHeight()：获取光标高度
   - childCount()：获取子项数量
   - childAt(int index)：获取指定索引的子项
4. **为 NumberItem 类重写光标定位方法**：继承 MathFormulaItem 的光标定位方法，支持文本偏移定位
5. **为 TextBlockItem 类添加视图-数据映射方法**：
   - getTextFragmentForSpan(InlineSpan*)：根据 TextSpan 获取对应的 TextFragment
   - getMathItemForSpan(MathSpan*)：根据 MathSpan 获取对应的 MathFormulaItem
   - clearContentItems()：清除内容项
   - 添加 m_textFragmentMap 和 m_mathItemMap 映射表

### 新增文件
- `include/graphics/cursor/UnifiedCursor.h`
- `src/graphics/cursor/UnifiedCursor.cpp`

### 修改文件
- `include/graphics/items/TextFragment.h`
- `src/graphics/items/TextFragment.cpp`
- `include/graphics/formula/MathFormulaItem.h`
- `src/graphics/formula/MathFormulaItem.cpp`
- `include/graphics/formula/NumberItem.h`
- `src/graphics/formula/NumberItem.cpp`
- `include/graphics/items/TextBlockItem.h`
- `src/graphics/items/TextBlockItem.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 20:30:00
### 实现内容
完成数学公式编辑器阶段五：TextBlockItem 集成。完全重写了 TextBlockItem 类，实现了从数据到视图的完整集成。

### 主要修改
1. **完全重写 TextBlockItem 类**：不再使用 QGraphicsTextItem，完全自己实现
   - 提供 buildContentItems() 从 ParagraphBlock 数据创建视图
   - 提供 performLayout() 使用 TextBlockLayoutEngine 执行布局
   - 建立公式映射（MathSpan到视图、偏移量到MathSpan）
   - 支持统一光标（getMathSpanAtOffset、getOffsetForMathSpan、getMathSpanRect 等）
2. **修改 InlineSpan 类**：让它继承自 QObject，解决 MathSpan 的继承问题
3. **修改所有 MathSpan 子类**：更新构造函数以接受 QObject* parent 参数
4. **修改 DocumentScene 类**：暂时注释掉 m_pageTextItems 相关代码，适配新的 TextBlockItem

### 修改文件
- `include/graphics/items/TextBlockItem.h`
- `src/graphics/items/TextBlockItem.cpp`
- `include/core/document/InlineSpan.h`
- `src/core/document/InlineSpan.cpp`
- `include/core/document/TextSpan.h`
- `src/core/document/TextSpan.cpp`
- `include/core/document/MathSpan.h`
- `src/core/document/MathSpan.cpp`
- `include/core/document/math/NumberMathSpan.h`
- `src/core/document/math/NumberMathSpan.cpp`
- `include/core/document/math/FractionMathSpan.h`
- `src/core/document/math/FractionMathSpan.cpp`
- `include/core/document/math/RowContainerMathSpan.h`
- `src/core/document/math/RowContainerMathSpan.cpp`
- `include/graphics/scene/DocumentScene.h`
- `src/graphics/scene/DocumentScene.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 19:50:00
### 实现内容
完成数学公式编辑器阶段四：视图层 MathFormulaItem。按照设计文档创建了所有公式图形项基类和主要的公式视图类。

### 主要修改
1. **创建 MathFormulaItem 基类**：作为所有公式图形项的基类，继承 QGraphicsItem
   - 提供 relayout() 方法用于重新计算布局
   - 提供 bindData() 方法用于数据绑定
   - 提供宽度、高度、基线等排版尺寸属性
   - 实现 boundingRect() 和 paint() 基类方法
2. **创建 NumberItem 类**：数字/变量图形项
   - 使用指定字体和颜色绘制文本
   - 使用 QFontMetricsF 计算文本尺寸
   - 支持基线对齐
3. **创建 RowContainerItem 类**：行容器图形项
   - 使用 MathFormulaItemFactory 创建子项
   - 实现横向布局，子项基线对齐
   - 支持间距设置
4. **创建 FractionItem 类**：分数图形项
   - 管理分子和分母项
   - 绘制分数线
   - 分子分母居中对齐
   - 计算整体尺寸
5. **创建 MathFormulaItemFactory 工厂类**：公式图形项工厂
   - 根据 MathSpan 类型创建对应的 MathFormulaItem 子类
   - 支持扩展其他类型的公式项

### 新增文件
- `include/graphics/formula/MathFormulaItem.h`
- `src/graphics/formula/MathFormulaItem.cpp`
- `include/graphics/formula/NumberItem.h`
- `src/graphics/formula/NumberItem.cpp`
- `include/graphics/formula/RowContainerItem.h`
- `src/graphics/formula/RowContainerItem.cpp`
- `include/graphics/formula/FractionItem.h`
- `src/graphics/formula/FractionItem.cpp`
- `include/graphics/factory/MathFormulaItemFactory.h`
- `src/graphics/factory/MathFormulaItemFactory.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 18:30:00
### 实现内容
完成数学公式编辑器阶段三：数据层公式元素。按照设计文档创建了 MathSpan 基类和主要的公式数据类。

### 主要修改
1. **创建 MathSpan 基类**：作为所有公式数据的基类，继承 InlineSpan 和 QObject
   - 采用 Composite 设计模式
   - 提供 contentChanged 信号
   - 提供子项管理接口（insertChild、appendChild、removeChild 等）
2. **创建 NumberMathSpan 类**：数字/变量数据
   - 存储文本内容
   - 提供 textChanged 信号
   - 支持 appendChar 和 removeLastChar 操作
3. **创建 RowContainerMathSpan 类**：行容器数据
   - 管理多个子项
   - 支持间距设置
   - 完整的子项管理
4. **创建 FractionMathSpan 类**：分数数据
   - 管理分子和分母
   - 完整的子项管理

### 新增文件
- `include/core/document/MathSpan.h`
- `src/core/document/MathSpan.cpp`
- `include/core/document/math/NumberMathSpan.h`
- `src/core/document/math/NumberMathSpan.cpp`
- `include/core/document/math/RowContainerMathSpan.h`
- `src/core/document/math/RowContainerMathSpan.cpp`
- `include/core/document/math/FractionMathSpan.h`
- `src/core/document/math/FractionMathSpan.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 17:30:00
### 实现内容
完成数学公式编辑器阶段二：文本视图实现。按照设计文档创建了 TextFragment 和 TextBlockLayoutEngine。

### 主要修改
1. **创建 TextFragment 类**：继承 QGraphicsItem，自己绘制文本，实现文本测量和绘制
   - 使用 QFontMetricsF 计算文本尺寸
   - 支持基线对齐
   - 字符样式应用
2. **创建 TextBlockLayoutEngine 类**：文本块布局引擎
   - 支持自动换行
   - 基线对齐（专业排版）
   - 文本对齐（左对齐、居中、右对齐）
   - 计算整体尺寸

### 新增文件
- `include/graphics/items/TextFragment.h`
- `src/graphics/items/TextFragment.cpp`
- `include/graphics/items/TextBlockLayoutEngine.h`
- `src/graphics/items/TextBlockLayoutEngine.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 17:00:00
### 实现内容
完成数学公式编辑器阶段一：基础框架搭建。按照设计文档实现了核心数据结构和类框架。

### 主要修改
1. **修改 BaseBlockItem**：从继承 QGraphicsRectItem 改为继承 QGraphicsItem，添加了 boundingRect() 和 paint() 的默认实现，以及兼容旧 QGraphicsRectItem 接口的 setRect()、rect() 方法
2. **创建 InlineSpan 基类**：作为所有内联内容（文本和公式）的基类
3. **创建 TextSpan 类**：继承自 InlineSpan，用于管理文本内容和样式
4. **修改 ParagraphBlock**：添加了 m_inlineSpans 列表和相应的管理方法（inlineSpanCount、inlineSpan、addInlineSpan、insertInlineSpan、removeInlineSpan、clearInlineSpans）
5. **修复现有类**：为 TextBlockItem、ImageBlockItem、TableBlockItem 添加了 boundingRect() 和 paint() 的实现，确保编译通过

### 新增文件
- `include/core/document/InlineSpan.h`
- `src/core/document/InlineSpan.cpp`
- `include/core/document/TextSpan.h`
- `src/core/document/TextSpan.cpp`

### 修改文件
- `include/graphics/items/BaseBlockItem.h`
- `src/graphics/items/BaseBlockItem.cpp`
- `include/core/document/ParagraphBlock.h`
- `src/core/document/ParagraphBlock.cpp`
- `include/graphics/items/TextBlockItem.h`
- `src/graphics/items/TextBlockItem.cpp`
- `include/graphics/items/ImageBlockItem.h`
- `src/graphics/items/ImageBlockItem.cpp`
- `include/graphics/items/TableBlockItem.h`
- `src/graphics/items/TableBlockItem.cpp`

### 编译状态
✅ 使用 CMakePresets.json 的 Debug 预设编译成功，无错误

## 2026-02-22 15:30:00
### 实现内容
创建了数学公式编辑器换行处理方案文档，详细描述了换行操作的核心原则、具体实现步骤、特殊情况处理以及与现有架构的兼容性。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\数学公式编辑器换行处理方案.md`

### 主要内容
1. **核心原则**：换行是段落级操作，公式作为内联元素不会跨段落存在
2. **段落拆分逻辑**：实现了光标定位、段落拆分、视图更新和撤销/重做支持
3. **特殊情况处理**：包括公式内部换行、空段落处理和格式继承
4. **兼容性**：保留原有接口，新系统与旧系统并行工作

## 2026-02-22 15:45:00
### 实现内容
更新了数学公式编辑器换行处理方案文档，新增了自动换行处理的详细内容。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\数学公式编辑器换行处理方案.md`

### 主要新增内容
1. **自动换行核心原则**：布局级操作，公式作为整体不跨多行
2. **自动换行算法**：由TextBlockLayoutEngine统一处理，计算每个内联元素宽度
3. **特殊情况处理**：公式宽度超过行宽、行首公式、行尾公式等情况
4. **视图层更新**：保持基线对齐，处理行间距和段落间距

## 2026-02-22 16:00:00
### 实现内容
创建了TextBlockLayoutEngine设计文档，详细描述了文本块布局引擎的类结构、实现细节和集成方式。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\TextBlockLayoutEngine设计文档.md`

### 主要内容
1. **核心功能**：处理文本和公式的布局、自动换行、基线对齐
2. **类结构**：头文件和实现文件的详细设计
3. **关键算法**：自动换行逻辑、基线对齐、行高计算
4. **集成方式**：与TextBlockItem的集成示例
5. **性能优化**：缓存机制和避免重复计算

## 2026-02-22 16:15:00
### 实现内容
创建了统一光标设计文档，详细描述了光标状态管理、移动逻辑和渲染方式。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\统一光标设计文档.md`

### 主要内容
1. **核心原则**：单一光标、无缝切换、所见即所得
2. **类结构**：UnifiedCursor类的设计和接口
3. **移动逻辑**：文本和公式内的光标移动算法
4. **渲染方式**：动态计算光标位置和高度
5. **关键技术**：避免双光标问题、公式内光标定位
6. **集成方式**：与DocumentView的集成示例

## 2026-02-22 16:30:00
### 实现内容
更新了统一光标设计文档，新增了字符插入实现的详细内容，包括输入法处理、统一插入逻辑和汉字插入优化。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\统一光标设计文档.md`

### 主要新增内容
1. **输入法处理**：处理QInputMethodEvent事件，支持中文输入法
2. **统一插入逻辑**：文本和公式内的字符插入统一接口
3. **汉字插入优化**：支持汉字作为变量名和文本内容
4. **输入法查询**：重写inputMethodQuery方法提供输入法所需信息

## 2026-02-22 16:45:00
### 实现内容
创建了数学公式编辑器布局更新机制文档，详细描述了宽度和高度变化的影响、信号通知机制、递归布局更新和性能优化策略。

### 文档位置
`d:\vscodeproject\QtWordEditor\u文档\数学公式编辑器布局更新机制.md`

### 主要内容
1. **核心问题分析**：宽度和高度变化对布局的影响
2. **信号通知机制**：内容变化时的信号传递
3. **递归布局更新**：从子项到父项的布局更新
4. **行高计算机制**：基于行内最高元素的行高计算
5. **自动换行处理**：行宽不足时的自动换行逻辑
6. **性能优化策略**：局部更新、批量更新和缓存机制