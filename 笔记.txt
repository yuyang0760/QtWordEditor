现在不能直接删除旧的 Cursor 文件 ，否则会导致大量编译错误。正确的步骤应该是：

1. 完善 UnifiedCursor 的所有功能 - 实现移动、编辑、选择等完整功能
2. 逐步替换 - 逐个模块替换旧 Cursor 的使用
3. 最后删除 - 确认所有模块都使用 UnifiedCursor 后，再删除旧文件

- moveUp() / moveDown() - 上下移动
- moveToStartOfDocument() / moveToEndOfDocument() - 文档首尾
- setPositionFromScenePoint() - 鼠标点击定位

### 1. 逐步恢复并完善 UnifiedCursor（推荐）
现在程序已经可以正常运行了，我们可以逐步恢复 UnifiedCursor 的功能，每次只启用一小部分并测试：

- 先恢复 UnifiedCursor 的基础初始化（不添加到场景，也不处理事件）
- 然后逐步添加光标显示
- 最后添加事件处理
### 2. 完善 UnifiedCursor 的缺失功能
根据之前的待办事项，还有以下功能需要完善：

- moveUp() 和 moveDown() 方法
- 完整的 setPositionFromScenePoint() 鼠标点击定位

选项 1 ：完善 UnifiedCursor 的缺失功能（moveUp/moveDown、setPositionFromScenePoint）
 选项 2 ：让 updateCursor 能正确定位到实际文本位置（和旧光标同步）
 选项 3 ：开始添加事件处理（键盘、鼠标事件）
  选项 4 ：其他（请告诉我）


  ### 选项 1：修改 EditEventHandler，使用 UnifiedCursor（推荐）
内容 ：修改 EditEventHandler 类，让它的 handleKeyPress、handleMousePress、handleInputMethod 等方法调用 UnifiedCursor 的方法，而不是旧 Cursor 的方法。

优点 ：这是最系统的方案，EditEventHandler 是统一的事件处理中心，改这里最符合架构。

工作量 ：中等，需要修改 EditEventHandler 的实现。

### 选项 2：直接修改 DocumentView 的事件处理
内容 ：修改 DocumentView 的 keyPressEvent、mousePressEvent、inputMethodEvent 等，让它们直接调用 UnifiedCursor 的方法，而不是发出信号给 EditEventHandler。

优点 ：更直接，不需要中间层。 缺点 ：破坏了当前的事件转发架构（DocumentView → EditEventHandler）。

### 选项 3：先完善 UnifiedCursor 的其他功能
内容 ：

- 完善 moveToStartOfLine、moveToEndOfLine、moveToStartOfDocument、moveToEndOfDocument 的完整实现
- 添加选择功能（Shift + 方向键等）
- 完善输入方法处理
优点 ：让 UnifiedCursor 功能更完整，然后再替换更稳妥